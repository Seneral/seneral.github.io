SOURCE :: "SOURCE"
BUILD :: "BUILD"
RELATIVEPATHS :: TRUE

{
	%HEADER :: "header.html"
	%FOOTER :: "footer.html"

	// To mark the current page in the navigation bar
	<a class="" href="$CURPAGE"> :: <a class="curNav" href="$CURPAGE">
	<a class="dropdownbtn" href="$CURPAGE"> :: <a class="dropdownbtn curNav" href="$CURPAGE">

	// Page metadata and links
	%HEADLINKS :: "$CURPAGE"
	%SCRIPTS :: "$CURPAGE"

	// Page Content
	%BANNER :: "$CURPAGE"
	%CONTENT :: "$CURPAGE"

	// To allow referencing these generic informations elsewhere on the page
	%ABOUTME :: "source/aboutme.html%CONTENT"
	%CONTACT :: "source/contact.html%CONTENT"

	// Filler Content
	%LOREMIPSUM :: "LoremIpsum.html"

}